#include <iostream>

using namespace std;

int main()
{
	int one = 1;
	int two = 2;
	// one과 two 를 1,2값을 넣어준다.
	int sum = one + two;
	//변수에 입력된 값을 더해 sum에 넣어준다.

	cout << "1 + 2 = " << sum << endl;
	//문자열 출력 후 계산된 sum 출력
	return 0;
}